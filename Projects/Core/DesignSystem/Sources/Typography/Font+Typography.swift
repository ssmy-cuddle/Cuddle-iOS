//
//  Typography.swift
//  DesignSystem
//
//  Created by mvldev7 on 7/3/24.
//

import Foundation
import SwiftUI

public extension Font {
    
    // MARK: NPS - Header
    
    static let npsHeader20: Font = .custom(NPS.header.name, size: 20)
    static let npsHeader18: Font = .custom(NPS.header.name, size: 18)
    static let npsHeader16: Font = .custom(NPS.header.name, size: 16)
    
    // MARK: NPS - Title
    
    static let npsTitle16: Font = .custom(NPS.title.name, size: 16)
    static let npsTitle14: Font = .custom(NPS.title.name, size: 14)
    static let npsTitle12: Font = .custom(NPS.title.name, size: 12)
    static let npsTitle10: Font = .custom(NPS.title.name, size: 10)
    
    // MARK: NPS - Body
    
    static let npsBody12: Font = .custom(NPS.body.name, size: 12)
    static let npsBody10: Font = .custom(NPS.body.name, size: 10)
    static let npsBody8: Font = .custom(NPS.body.name, size: 8)
    
    // MARK: Pretendard - Header
    
    static let pretendardHeader20: Font = .custom(Pretendard.header.name, size: 20)
    static let pretendardHeader18: Font = .custom(Pretendard.header.name, size: 18)
    static let pretendardHeader16: Font = .custom(Pretendard.header.name, size: 16)
    
    // MARK: Pretendard - Title
    
    static let pretendardTitle16: Font = .custom(Pretendard.title.name, size: 16)
    static let pretendardTitle14: Font = .custom(Pretendard.title.name, size: 14)
    static let pretendardTitle12: Font = .custom(Pretendard.title.name, size: 12)
    static let pretendardTitle10: Font = .custom(Pretendard.title.name, size: 10)
    
    // MARK: Pretendard - Body
    
    static let pretendardBody14: Font = .custom(Pretendard.body.name, size: 14)
    static let pretendardBody12: Font = .custom(Pretendard.body.name, size: 12)
    static let pretendardBody10: Font = .custom(Pretendard.body.name, size: 10)
    static let pretendardBody8: Font = .custom(Pretendard.body.name, size: 8)
    static let pretendardBody6: Font = .custom(Pretendard.body.name, size: 6)
}
